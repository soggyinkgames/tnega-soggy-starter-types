Pattern: local-first adapter boundary

Build local implementations first behind stable contracts.
Resolve tools by catalog, not by raw implementation import.
Let orchestration select collections.
Let runtime execute through shared contracts.
Later replace local implementations with API-backed ones without changing agent-facing behavior.