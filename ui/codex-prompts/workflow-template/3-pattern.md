Pattern: <short pattern name>

Build <current implementation approach> behind <stable boundary>.
Resolve <capability> by <stable mechanism>, not by <fragile mechanism>.
Let <selection/composition owner> select <collections or units>.
Let <runtime owner> execute through <shared boundary>.
Later replace <current implementation source> with <future implementation source> without changing <consumer-facing behavior/contracts>.