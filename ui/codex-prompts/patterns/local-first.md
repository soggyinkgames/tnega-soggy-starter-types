Pattern: local-first adapter boundary

Build local implementations first behind stable contracts.
Resolve capabilities by catalog, not by direct file coupling.
Let the composition layer select collections.
Let runtime execute through shared contracts.
Later replace local implementations with service-backed implementations without changing consumer-facing behavior.