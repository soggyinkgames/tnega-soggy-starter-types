Pattern: scaffold-contract separation

Keep templates focused on structure, not runtime ownership.
Resolve runtime behavior from stable contracts, not from scaffold-specific logic.
Let generated outputs plug into existing system boundaries.
Let execution happen through the real runtime path.
Later evolve scaffolding without changing the runtime contract.