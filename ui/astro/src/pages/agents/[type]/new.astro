---
import BaseLayout from "../../../layouts/BaseLayout.astro";
const { type } = Astro.params;

const sampleBody = {
  query: "What is Soggy Starter?",
  maxResults: 5,
  threshold: 0.7,
};
---
<BaseLayout title={`Create • ${type}`}>
  <main class="panel create">
    <h1>Create Agent from Template</h1>
    <div class="meta">Type: <b>{type}</b></div>
    <p>This page will trigger the agent generator and wire it to the API. For now, it shows a sample payload and endpoint for the Knowledge agent.</p>

    <div class="card">
      <div class="label">Endpoint</div>
      <code>POST /agents/knowledge</code>
    </div>
    <div class="card">
      <div class="label">Sample Body</div>
      <pre>{JSON.stringify(sampleBody, null, 2)}</pre>
    </div>
    <a class="cta" href="/">Back to dashboard</a>
  </main>
  <style>
    .create { padding:22px; }
    h1 { margin:0; font-size:1.6rem; }
    .meta { margin-top:6px; color: var(--muted); }
    p { color: var(--muted); }
    .card { margin-top:14px; padding:12px; border-radius:12px; background: var(--card-grad-1); border:1px solid rgba(255,255,255,.06); }
    pre { margin:0; white-space:pre-wrap; }
    .cta { display:inline-block; margin-top:16px; padding:10px 14px; border-radius:12px; background: var(--brand); color: #0b0f16; font-weight:800; }
  </style>
</BaseLayout>

