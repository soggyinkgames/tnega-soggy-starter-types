# Creative Generation Scaffold

- Goal profile: music
- Input kinds: prompt-text, audio, reference-set
- Output targets: music
- Default orchestration: sequential

## Next checks
- Confirm the declared `inputKinds` match the real source materials this agent should accept.
- Confirm the declared `outputTargets` match the artifact class this agent should produce.
- Keep runtime tool selection behind sequential orchestration and the declared inputs and outputs.
